pgpool:
 build: ./pgpool
 container_name: pgpool
 ports:
  - "9999:9999"
 links:
  - db
  - server
db:
 build: 
 container_name: postgis
 ports:
  - "5432:5432"
 volumes:
  - ~/postgres_data:/var/lib/postgresql
server:
 build: ./geoserver
 container_name: geoserver
 ports:
  - "8080:8080"
